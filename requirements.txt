Pillow>=9.0.0
requests>=2.0.0
pyttsx3>=2.90
numpy>=1.21.0

# Web UI
Flask>=2.0.0
flask-cors>=3.0.0
gunicorn>=20.0.0

# CORS for webapp
flask-cors>=3.0.10

# Optional utilities
# waitress for production Windows WSGI server
# TTS (Coqui): TTS

# Optional (install if you want higher-quality local models):
# TTS (Coqui): TTS
# diffusers, accelerate, safetensors for Stable Diffusion
pytest>=7.0.0